__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
venv/
*.egg-info/
dist/
build/

# Build outputs
asdd-bundle.tar.gz
bundle-staging/

# Subscription credential store (spec 009) — live OAuth tokens, never commit
_state/claude-auth/
**/claude-auth/

# OS noise
.DS_Store

# Editor noise
.idea/
.vscode/

# Claude Code agent folder — may contain credentials or local settings
.claude/
# …but the project skeleton ships an intentional, credential-free permission
# guardrail file that MUST be tracked (spec 006). Re-include just that path.
!project_skeleton/.claude/
!project_skeleton/.claude/settings.json
CLAUDE.md
