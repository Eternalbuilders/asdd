"""ASDD — Agentic Spec-Driven Development.

CLI + per-project container manager that isolates multiple spec-driven
projects on a single host. Named ``asdd`` rather than ``platform`` to
avoid shadowing the Python stdlib ``platform`` module.
"""

__version__ = "0.1.0"
